<h1> Episode <%= @episode.number.to_s %> Info </h1>

<h3>
    Date: <%= @episode.date %>
</h3>

<ul>
  <% @episode.guests.each do |guest| %>
      <li>
          <%= link_to guest.name, guest_path(guest) %>
      </li>
  <% end %>
</ul>
