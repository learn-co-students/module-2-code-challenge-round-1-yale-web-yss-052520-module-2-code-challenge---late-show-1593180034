<%= link_to "<<All Episodes", episodes_path %>

<h1>Episode <%= @episode.number %> - <%= @episode.date.strftime("%B %d, %Y") %></h1>

Average rating: <%= @episode.average_rating %>

<h2>Appearances:</h2>

<ul>
    <% @episode.guests.each do |guest| %>
        <li><%= link_to guest.name, guest_path(guest) %></li>
    <% end %>
</ul>

<%= link_to "Add a new appearance", new_appearance_path %>