<h3>New Appearance Form:</h3>

<% if flash[:errors] %>
<ul>
    <% flash[:errors].each do |e|%>
        <li><%= e %></li>
    <%end%>
<%end%>
</ul>
<%= form_for @appearance do |f|%>

    <%= f.label :guest %>
    <%= f.collection_select :guest_id, @guests, :id, :name %>

    <%= f.label :episode %>
    <%= f.collection_select :episode_id, @episodes, :id, :number %>

    <%= f.label :rating %>
    <%= f.number_field :rating %>

    <%= f.submit %>

<%end%>